[package]
name = "net_stack_server"
version = "0.1.0"
edition = "2021"
description = "GaxNet Ring-3 User-Space Protocol Server (Ethernet, ARP, IPv4/IPv6, ICMP, UDP, TCP)"

[[bin]]
name = "net_stack_server"
test = false

[dependencies]
gaxera-abi = { path = "../gaxera-abi" }
libgaxera = { path = "../libgaxera" }
net_types = { path = "../net_types" }

[lints]
workspace = true
