[package]
name = "ramfs"
version = "0.1.0"
edition = "2024"

[dependencies]
kernel-core = { path = "../kernel-core" }
gaxera-abi = { path = "../gaxera-abi" }

[dev-dependencies]
proptest = "1.0"

[lints]
workspace = true
