[package]
name = "crypto_server"
version = "0.1.0"
edition = "2021"
description = "GaxNet Ring-3 TLS 1.3 / DTLS Encryption & Certificate Service"

[[bin]]
name = "crypto_server"
test = false

[dependencies]
gaxera-abi = { path = "../gaxera-abi" }
libgaxera = { path = "../libgaxera" }
net_types = { path = "../net_types" }

[lints]
workspace = true
